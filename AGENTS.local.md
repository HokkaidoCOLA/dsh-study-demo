# 学习演示规范（给 AI 读）

> 这个文件与 `AGENTS.md`（人物卡）**分开维护**，互不合并。
> 人物卡管「怎么跟我说话」，这里管「怎么给我做演示」。
> 详细施工图（含可抄的代码骨架）在 `演示规范.md`。
> **可复现的流程、自检工具、故障手册在 `演示流程.md`** —— 动手前读一遍，推送前照第 8 节清单勾一遍。

## 硬规则

- 讲题、讲知识点时优先用 `demo` 工具拉侧边栏演示。**动手写 HTML 前先读 `演示规范.md`**。
- **演示命名有固定格式**：`科目-册-章节-知识点-编号`（例 `数学-选择性必修一-空间向量-空间向量的加减-01`），它同时是文件名和 `<title>`；改了内容就换编号。**内容确实是官方教材里的（人教版/北师大版…）才写册名；不是教材内容、或是跨章节专题/拓展/考法总结 → 第 2 段写「专题」、第 3 段写内容范畴**（例 `物理-专题-时空观-绝对空间与水桶实验-01`）；拿不准一律用「专题」，不许硬套册章。细则见 `演示流程.md`。
- **推送前必须自检**：`node tools/check-demo.mjs <演示.html>` 退出码必须是 0，并且要亲眼看一遍 `tools/shots/` 里的关键帧（起点/突变点/终点）——数值全过不等于看起来对。流程见 `演示流程.md`。
- **题目涉及随时间变化的过程 → 演示必须带时间轴**：
  1. 能自动播放（▶/❚❚），播完自动停，再点从头；
  2. 能手动拖动到任意时刻，拖动时自动暂停播放；
  3. 读数要说清「现在是哪一刻 + 状态名 + 关键数值」；
  4. 关键节点（开始／突变／结束）要有可点的一键跳转标签；
  5. 画面、曲线、数值必须跟着时间轴同步重绘。
- 演示页面默认只有 300–500px 宽：纵向排布，一屏看完，**不出现横向滚动**。
- 3D 演示必须带降级：three.js 加载或 WebGL 失败时，退回 Canvas 2D 斜投影，不能留空白。
