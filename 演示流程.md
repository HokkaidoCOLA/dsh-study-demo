# 演示动画制作流程（可复现版）

> 和 `AGENTS.local.md`（硬规则）、`演示规范.md`（施工图 + 代码骨架）配套。
> 这份管**流程本身**：怎么保证「做出来的演示一定能看、不出错、下次还能重来一遍」。
> 所有条目都来自 2026-09 那一轮真实踩过的坑，不是设想。

---

## 0. 一句话流程

**写一个自包含 HTML → 无头自检（5 项，退出码判定）→ 亲眼看关键帧 → 推送到侧边栏 → 对推送后的那个文件再验一次。**

五步缺一不可。跳过第 3 步 = 拿用户侧边栏当试验场；跳过第 4 步 = 数值全过但版式烂；跳过第 5 步 = 验证的和推送的可能不是同一个文件。

---

## 1. 一次性准备

| 东西 | 位置 | 说明 |
|---|---|---|
| 自检脚本 | `tools/check-demo.mjs` | 5 项检查 + 退出码；出图到 `tools/shots/` |
| 排错脚本 | `tools/diag-demo.mjs` | 把演示自己抓到的错误原文打出来 |
| 依赖 | `tools/node_modules` | `cd tools && npm install --cache /tmp/npmcache` |

- **`--cache /tmp/npmcache` 不能省**：沙箱下 `~/.npm` 不可写，不指缓存目录会 EPERM。
- 自检依赖 `@napi-rs/canvas`（无头真 Canvas），并自动把系统 `STHeiti` 注册成 `PingFang SC`。
- **没注册中文字体时中文会画成豆腐块** —— 那种情况下"看图验证"等于没看，脚本会警告。

---

## 2. 五步流程

### 第 1 步 · 定内容（先判断该不该做动画）
- 有**随时间连续变化的量**才做动态演示；没有就退化成"结构图 + 步进"或干脆别做。
- 每个科目能真动的东西（经验值）：物理=过程动画、地理=剖面推进（冷锋/环流）、化学=装置过程（滴液/变色）、生物=分裂/曲线、数学=动点轨迹、英语=时间流逝+动作长短、语文=叙述顺序（唯一能动的角度）。
- 一次讲解最多 1–2 个演示。

### 第 2 步 · 写 HTML
- 自包含、**零外部依赖**（不引 CDN，一断网就是白屏）。
- 骨架照抄现成的四个之一：`.dsh-demo/化学-选择性必修一-水溶液中的离子反应与平衡-中和滴定-01.html`（场景+曲线）、`数学-选择性必修一-圆锥曲线的方程-椭圆的定义-01.html`（单画布+曲线）、`语文-专题-记叙文写作-叙述顺序-01.html`（双行结构图）、`地理-选择性必修一-大气的运动-冷锋过境-02.html`（宽场景+曲线）。
- 必做三件事，否则自检直接失败：
  1. **每帧先 `clearRect`**（不清就是叠影）；
  2. 暴露 `window.__probe`（见第 3 节）；
  3. 用 `try/catch` 把错误写进 `#err` 面板（不要静默吞掉）。

#### 命名规则：文件名 = `title` = `<title>`（硬规则）

格式 **`科目-册/专题-章节/范畴-知识点-编号`**。第 2、3 段有两种写法，**按内容是否属于官方教材二选一**：

**① 确实是教材内容**（人教版 / 北师大版 / 苏教版 / 外研版……某一册某一章）→ 照教材写：

```
数学-选择性必修一-空间向量-空间向量的加减-01.html
地理-选择性必修一-大气的运动-冷锋过境-02.html
语文-必修上册-第六单元-劝学论证思路-01.html
化学-选择性必修一-水溶液中的离子反应与平衡-中和滴定-01.html
```

**② 不是教材内容**（跨章节专题、拓展/科学漫步、考法总结、竞赛或科普、教材外补充）→ 第 2 段写 **`专题`**，第 3 段写**内容范畴**（不是章名）：

```
物理-专题-时空观-绝对空间与水桶实验-01.html
英语-专题-动词时态-过去进行时与when从句-01.html
语文-专题-记叙文写作-叙述顺序-01.html
```

- **拿不准算不算教材内容 → 一律用「专题」形式。** 宁可少写一个册名，也不要写错册章：写错了会长期误导复习时的索引。
- 同时**不要为了凑格式去改演示内容** —— 命名服从内容，不是内容服从命名。演示讲什么，名字就概括什么。

其余规则：

- 插件按**标题**生成文件名（保留中文，只清掉 `/ \ : * ? " < > |` 与控制符）—— 所以 `demo` 工具的 `title` 必须照这个格式写，别写「二次函数演示」。
- **三处名字要完全一致**：文件名、HTML 里的 `<title>`、`demo` 的 `title`。自检脚本查文件名格式、第 2 段是否为教材册名或「专题」，并在这两处及 `<title>` 不一致时告警。
- 字段写法：
  - **科目**：物理 / 数学 / 英语 / 语文 / 化学 / 生物 / 地理
  - **第 2 段**：教材册名（必修一、选择性必修一……；语文用 必修上册 / 必修下册）**或** `专题`
  - **第 3 段**：教材章名（`大气的运动`、`圆锥曲线的方程`、语文的 `第六单元`）**或**内容范畴（`时空观`、`动词时态`、`记叙文写作`）
  - **知识点**：≤10 字简称（`冷锋过境`、`中和滴定`、`椭圆的定义`）
  - **编号**：两位数字，同一知识点下的第几份，从 `01` 起
- **改了内容就换编号**（`-01` → `-02`）。同名会覆盖同一个文件，已经开着的标签页要刷新才变；同一知识点的历次迭代就靠编号区分（例：水桶实验 `物理-专题-时空观-绝对空间与水桶实验-01…05`）。
- 手工改名后，对话里旧卡片的「在侧边栏打开」会指向旧路径而失效 —— 关掉旧标签页，改用新文件。

### 第 3 步 · 自检
```bash
node tools/check-demo.mjs <演示.html> [画布CSS宽] [画布CSS高]
echo $?          # 0 = 全过；1 = 有失败，不要推送
```
失败时先跑排错：
```bash
node tools/diag-demo.mjs <演示.html>     # 打印 #err 面板原文
```

### 第 4 步 · 亲眼看关键帧（**唯一能判断「是不是真动态」的一步**）
- **判据：把游标和高亮遮住，画面里还剩什么在动？** 如果答案是「没有了」，那就不是动态演示，只是带游标的静态图。
- 场景里至少要有一个**随时间连续变化的几何量**：位置（冷锋推进、动点绕行）、长度（液面下降、焦半径变长）、数量（细胞分裂）、颜色（指示剂变色）。
- 出图在 `tools/shots/<画布id>-<时刻>.png`，**至少看三帧：起点 / 突变点 / 终点**。
- 看什么：有没有叠字、超界、豆腐块、曲线压标题、标注互相盖住、形状画错。
- 这一步是唯一能发现"数值全对但看起来是错的"的环节，不能省。

### 第 5 步 · 推送 + 复验
1. 用 `demo` 工具推送（`title` / `html` / `focus`）。
2. 比 sha256：`shasum -a 256 .dsh-demo/<推送文件> /tmp/<验证文件>` —— 两个必须相同。
3. **对推送后的文件再跑一次自检**（路径直接指向 `.dsh-demo/…`），确认落地的那份也过。
4. 确认落盘文件名就是标题本身（`ls .dsh-demo/`）；同名会覆盖，所以要换编号就得在 `title` 里换。
5. 告诉用户打开的是**新标签页**（旧标签页会留在侧边栏，内容不会自动更新）。

---

## 3. probe 接口契约（可复现的关键）

演示必须暴露这个对象，自检脚本靠它驱动：

```js
window.__probe = {
  setT(t),            // 必填：把时间轴设到 t 并重绘（拖动/播放都走它）
  frames: [0, 3, 7],  // 选填：要检查的时刻；默认取 [0, 中点, 终点]
  tMax,               // 选填：时间轴终点
  size(),             // 选填：主画布 CSS 尺寸 [w,h]
  chartSize?()        // 选填：第二个画布尺寸
};
```

配套约定：
- 时间轴控件（▶ / range / 关键节点按钮）最终都调用同一个 `setT`；
- `setT` 内部只做「算状态 → 重绘」，不存增量状态，这样**任意时刻都能重算**，拖动不会漂；
- 动画里所有"随时间变化"的东西都写成 **t 的函数**（雨滴、脉冲圈、分裂进度），不要用 `+=` 累加，否则拖动时间轴会不一致。

---

## 4. 判定标准表（`tools/check-demo.mjs` 的实际阈值）

| 检查项 | 阈值 | 为什么 |
|---|---|---|
| 命名规范 | 文件名必须匹配 `科目-册/专题-章节/范畴-知识点-编号` | 文件名就是索引；第 2 段非册名且非「专题」→ 告警；`<title>` 不一致 → 告警 |
| 错误面板 | 必须为空 | 首帧抛错会让后面每帧都静默失败，画面停在半成品 |
| 残影 | 逐像素完全相同 | 「先画 t0 再画 t1」== 「干净画布只画 t1」 |
| 画面变化（定帧检测） | 相邻帧画面变化 ≥ **0.8%** | **只能排除定帧/白屏**；区分不了「游标在动」和「场景在动」（实测：旧的静态英语演示也拿到 10–28%）——真动态只能靠看图判断 |
| 墨迹 | ≥ 800 非透明像素 | 太少 = 白屏 |
| 文字出界 | ≤ **2px** | 文字被切掉是真的错 |
| 图形出界 | ≤ **40px** | 图形本来就可能有部分在视口外，宽容对待 |

---

## 5. 故障手册（全部真踩过）

| 症状 | 根因 | 哪个检查抓到 | 改法 |
|---|---|---|---|
| 画面一层层叠影 | 没 `clearRect` | 残影 | 每帧先清 |
| 画面被拉伸、位置错乱 | 侧边栏展开改了 iframe 宽度，**不触发 `window.resize`** | 肉眼 | 加 `ResizeObserver` |
| 动态性 0%、图元 0、墨迹偏少 | 首帧就抛错：`rgba(PAL.short,…)` 这类**不存在的颜色**→ `undefined[0]` | 动态性 + `diag` | 颜色统一放 `COL`，用 `hex2rgb` 转成数组；`PAL` 只存字符串 |
| 中文全变豆腐块 | 无头环境没注册中文字体 | 字体自检 | 脚本已自动注册 STHeiti |
| 下标 ₁ ₂ 变豆腐块 | 中文字体缺 U+2081/2082 | 看图 | 用普通数字（F1/F2） |
| 图形画到画布外、形状错 | 多边形顶点没裁进世界范围（锋面跑出画面后） | 图形出界 + 看图 | 顶点一律 `clamp` 到世界框 |
| 曲线冲出图外后贴着顶边成横线 | 裁剪后继续连线 | 看图 | 超出范围就**断笔**，别贴边画 |
| 曲线压住面板标题 | y 轴范围没留余量 | 看图 | 上下各留 15%–20% |
| 末位刻度和单位叠字 | 刻度画到最后一个点 + 单位右对齐 | 看图 | 少画一个刻度 |
| 左侧行标签被裁 | 左边预留宽度不够 | 文字出界 | `X0` 按最长标签留（≈80px） |
| 判断恒定/守恒的量有误差 | 公式写错 | 单独写数值断言（如 r1+r2 ≡ 2a） | 在自检之外再做一次纯数学校验 |
| 推送的和验证的不是同一份 | 验证后又改了文件 | sha256 对比 | 推完立刻比 sha，再对推送文件复验 |
| **自动检查说「在动」，其实是静态图 + 游标** | 像素变化率区分不了「游标移动」与「场景演化」；高亮条换一行就是 10%+ 的像素变化 | **自动检查抓不到** | 靠第 4 步人工判据；场景里必须有一个随时间连续变化的几何量 |

---

## 6. 版式硬规则（窄栏 300–500px）

- 纵向排布，**绝不出现横向滚动**；`wrap` 最大 520px。
- 布局二选一：宽场景（剖面/时间轴）→ **上场景 + 下曲线**；方形场景 → **左图表 42% + 右场景**，栏宽 <380px 自动上下堆叠。
- 画布高度：`Math.max(150, Math.min(240, Math.round(w * 0.46~0.85)))`；两个画布加起来别超过 ~420px。
- 字号：标题 15 / 正文 11.5 / 读数 11.5 / 画布内 9.5–11 / 图例 9。
- 图表里的文字加 2.5px 底衬（halo），**画布内坐标轴数字不加**（会糊成一团）。
- 明暗两套颜色写进 CSS 变量，canvas 里用 `getComputedStyle` 读，别在 canvas 里写死黑色。
- 颜色只在两处定义：CSS 变量（给 HTML 用）+ `PAL/COL`（给 canvas 用），**两边名字要能对上**。

---

## 7. 时间轴六条（用户硬要求，逐条对照）

1. 能自动播放（▶/❚❚），播完自动停，再点从头；
2. 能手动拖到任意时刻，拖动时**自动暂停**；
3. 读数说清「哪一刻 + 状态名 + 关键数值」；
4. 关键节点可一键跳转；
5. 画面、曲线、数值跟时间轴同步重绘；
6. 一次完整过程 8–15 秒放完。

---

## 8. 一页清单（推送前逐条勾）

- [ ] **名字**：`title` / 文件名 / `<title>` 三处一致；**是教材内容就写册+章，不是就写「专题」+内容范畴，拿不准用「专题」**；改了内容换了编号
- [ ] 内容本身有「随时间变化的量」吗？（没有就别硬做动画）
- [ ] 零外部依赖；每帧 `clearRect`；暴露 `__probe`；有 `#err` 面板
- [ ] 所有随时间变化的东西都是 **t 的函数**（不是累加）
- [ ] `node tools/check-demo.mjs …` **退出码 0**
- [ ] 看过起点/突变点/终点三帧：无叠字、无超界、无豆腐块、无形状错
- [ ] **人工判据**：遮住游标/高亮后画面里还剩什么在动？（「没了」= 不是动态演示，退回去重做）
- [ ] 有守恒量的话，另外用纯数学断言验过一次
- [ ] `demo` 推送后：sha256 一致 + **对推送文件再跑一次自检**
- [ ] 提醒用户开新标签页
